1、本示例用于展示同步仿真发送数据与接收数据。
2、使用时，用二次开发模块加载动态链接库文件“MyModule.dll”
3、参数设置可根据实际网络情况进行设置。
4、选择工作模式为“发送”或“接收”。
5、使用时，需要作为接收方先创建服务，然后发送方来连接到接收方。
6、先设置好参数和启动服务后才可启动仿真。
7、发送方数据默认从二次开发模块的“IN1”输入数据。
8、接收方数据默认从二次开发模块的“OUT1”输出数据。
9、未尽事宜请参考源代码中注释并熟知源代码的工作原理。